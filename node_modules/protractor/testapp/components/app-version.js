'use strict';

angular.module('dashboardApp.appVersion', []).
  value('version', '0.1').
  directive('appVersion', ['version', function(version) {
    return function(scope, elm, attrs) {
      elm.text(version);
    };
  }]);
